#include <iostream>
using namespace std;

int main()
{
    int T, N;
    cin >> T;
    for (int x = 1; x <= T; x++)
    {
        cin >> N;
        int A[N];
        int B[N];
        string A1, A2;
        cin >> A1;
        cin >> A2;
        int length = A1.size();
        for (int y = 0; y < length; y++)
        {
            if (A1[y] != " ")
        }

        for (int y = 0; y < N; y++)
        {
        }
    }
}