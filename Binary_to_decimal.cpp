#include <iostream>
using namespace std;

int main()
{
    int num, counter = 0;
    cout << "enter the binary number : ";
    cin >> num;
    while(num > 0)
    {
        int rem = num % 10;

    }
}